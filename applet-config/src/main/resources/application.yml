server:
  port: 1201

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/lyq3/springcloud-applet-config.git
          search-paths: /{application}
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:1001/eureka/
